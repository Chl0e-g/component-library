import { Meta } from "@storybook/addon-docs/blocks";

import { RadiusScale } from "./TokenSpecimens.tsx";

<Meta title="Reference/Design Tokens/Radius" />

# Radius

Corner-rounding scale from [`src/tokens/radius.ts`](../radius.ts), generated as
`--radius-*` (in `px`).

<RadiusScale />
