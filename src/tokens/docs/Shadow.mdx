import { Meta } from "@storybook/addon-docs/blocks";

import { ShadowScale } from "./TokenSpecimens.tsx";

<Meta title="Reference/Design Tokens/Shadow" />

# Shadow

Elevation scale from [`src/tokens/shadow.ts`](../shadow.ts), generated as
`--shadow-*`. Use `sm` for resting surfaces, `md` for cards, `lg` for
overlays and modals.

<ShadowScale />
