import { Meta } from "@storybook/addon-docs/blocks";

import { SpacingScale } from "./TokenSpecimens.tsx";

<Meta title="Reference/Design Tokens/Spacing" />

# Spacing

A single t-shirt scale in [`src/tokens/spacing.ts`](../spacing.ts), generated as
`--spacing-*` (in `px`). Used for padding, gaps, and layout throughout.

<SpacingScale />
